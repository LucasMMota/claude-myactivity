---
description: (Re)run the claude-myactivity first-run setup to update your preferences
allowed-tools: Bash(./setup.sh:*)
---

Run the setup script so the user can (re)configure their preferences (ticket
convention, iTerm window size). It is interactive in the terminal.

```
!`./setup.sh`
```

Report where the config was written (`myactivity.config`) and what changed.
